{
  "name": "cosmic-zebra-refactor-2025",
  "displayName": "Cosmic Zebra Refactor",
  "description": "A unique minimal VSCode extension for refactoring experiments",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.101.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "cosmic-zebra-refactor.quantumSplit",
        "title": "Quantum Split Analysis"
      }
    ]
  },
  "scripts": {
    "build": "bun build src/extension.ts --outdir=dist --target=node --format=cjs --external=vscode --sourcemap=linked",
    "build:watch": "bun build src/extension.ts --outdir=dist --target=node --format=cjs --external=vscode --sourcemap=linked --watch",
    "package": "bun build src/extension.ts --outdir=dist --target=node --format=cjs --external=vscode --minify --production",
    "lint": "biome check . --write --unsafe",
    "format": "biome format . --write",
    "typecheck": "tsgo --noEmit",
    "test": "bun test",
    "check": "bun run format && bun run lint && bun run typecheck",
    "clean": "code --uninstall-extension undefined_publisher.cosmic-zebra-refactor-2025 && rm -f *.vsix && rm -rf dist/*",
    "reinstall": "bun run clean && bun run build && bunx vsce package && code --install-extension *.vsix --force",
    "restart-ext": "osascript -e 'tell application \"Visual Studio Code\" to activate' -e 'tell application \"System Events\" to keystroke \"p\" using {command down, shift down}' -e 'delay 0.5' -e 'tell application \"System Events\" to keystroke \"restart ext\"' -e 'delay 0.5' -e 'tell application \"System Events\" to key code 36'",
    "trigger": "curl -X POST http://localhost:3141/quantumSplit",
    "cli": "bun run src/cli.ts",
    "cli:help": "bun run src/cli.ts --help"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/bun": "latest",
    "@types/minimist": "^1.2.5",
    "@types/node": "20.x",
    "@types/vscode": "^1.101.0",
    "@typescript/native-preview": "^7.0.0-dev.20250619.1",
    "@vscode/vsce": "^3.5.0",
    "typescript": "^5.8.3"
  },
  "private": true,
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
